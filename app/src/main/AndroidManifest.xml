<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.incepted.cryptoaddresstracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_light">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Activities.DetailActivity" />
        <activity android:name=".Activities.TxActivity" />
        <activity android:name=".Activities.TokenAddressActivity" />
        <activity android:name=".Activities.TxDetailActivity" />
        <activity android:name=".Activities.NewAddressActivity" />
        <activity android:name=".Activities.TokenTransferActivity" />
        <activity android:name=".Activities.QRScanActivity"></activity>
    </application>

</manifest>